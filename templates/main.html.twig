{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    {% for note in notes %}
        <div class="row">
            <div class="col">
                <h1>{{ note.title }}</h1>
                <div>
                    {{ note.content }}
                </div>
            </div>
        </div>
    {% endfor %}
{% endblock %}
